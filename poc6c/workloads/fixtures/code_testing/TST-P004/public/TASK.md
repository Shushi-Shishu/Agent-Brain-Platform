# Add high-value tests for `weighted_average`

Create `test_candidate.py` with standard-library `unittest` tests. Do not modify
`weighted.py`; focus on numerical behavior and input validation at its public
function boundary.

Constraints:

- add at most 8 test functions or methods;
- each execution must finish within 3 seconds;
- use deterministic finite examples and explicit exception assertions;
- do not read or inspect source files or bytecode;
- do not depend on third-party packages.

The score rewards distinct numerical or validation faults exposed while the
supplied implementation remains passing and stable.
